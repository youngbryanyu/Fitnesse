{
  "ENV": "$NODE_ENV::string",
  "PORT": "$PORT::number",
  "REFRESH_TOKEN_SECRET": "$REFRESH_TOKEN_SECRET::string",
  "ACCESS_TOKEN_SECRET": "$ACCESS_TOKEN_SECRET::string",
  "PASSWORD_SECRET": "$PASSWORD_SECRET::string",
  "RETRY_LIMIT": 2,
  "MONGO_DB": {
    "CONNECTION_RETRIES": 2,
    "CONNECTION_RETRY_TIMEOUT": 1000,
    "CONNECTION_URL": "$MONGO_CONNECTION_URL::string"
  },
  "REDIS": {
    "HOST": "$REDIS_HOST::string",
    "PORT": "$REDIS_PORT::number",
    "PASSWORD": "$REDIS_PASSWORD::string",
    "CONNECTION_RETRIES": 2,
    "CONNECTION_RETRY_TIMEOUT": 1000
  },
  "RATE_LIMITING": {
    "USER": {
      "POST": {
        "THRESHOLD": 60,
        "WINDOW": 60000
      },
      "PUT": {
        "THRESHOLD": 60,
        "WINDOW": 60000
      },
      "GET": {
        "THRESHOLD": 60,
        "WINDOW": 60000
      },
      "DELETE": {
        "THRESHOLD": 60,
        "WINDOW": 60000
      }
    },
    "HEALTH_CHECK": {
      "GET": {
        "THRESHOLD": 120,
        "WINDOW": 60000
      }
    }
  },
  "AUTH": {
    "MAX_FAILED_LOGINS": 10,
    "LOCKOUT_TIME": 900,
    "FAILED_LOGIN_ATTEMPTS_WINDOW": 900,
    "ACCESS_TOKEN_LIFETIME": "15m",
    "IDLE_TIMEOUT": 172800
  },
  "FOOD_TRACKER": {
    
  }
}